export const theme = {
    extend: {
        fontFamily: {
            'rosario': ['Rosario', 'sans-serif'], // Usa el mismo nombre que en Google Fonts
        },
    },
};
export const variants = {};
export const plugins = [];